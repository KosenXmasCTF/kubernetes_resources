apiVersion: apps/v1
kind: Deployment
metadata:
  name: bad_path
  labels:
    app: bad_path
spec:
  selector:
    matchLabels:
      app: bad_path
  template:
    metadata:
      labels:
        app: bad_path
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: socat
          image: docker.pkg.github.com/kosenxmasctf/bad_path/bad_path:0.1.1
